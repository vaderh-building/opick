# Privy application ID
VITE_PRIVY_APP_ID=cmnjpca6600sj0cl5uj1wfool

# Dev-only — Hardhat wallet quick-connect on localhost
VITE_DEV_MODE=true

# OPick Oracle API base (no trailing slash). Leave unset to use local mock data.
# Fallback: NEXT_PUBLIC_ORACLE_URL is also honored (per spec).
VITE_ORACLE_URL=https://muslim-insulin-produce-discuss.trycloudflare.com
# NEXT_PUBLIC_ORACLE_URL=https://muslim-insulin-produce-discuss.trycloudflare.com

# Force mock data even when VITE_ORACLE_URL is set
VITE_USE_MOCK=true

# Legacy V5 backend (for /legacy pages only)
VITE_API_URL=http://localhost:3001
